# Solo datos de entrenamiento. Nunca pongas credenciales reales de
# producción en un Secret usado para un workshop como este.
#
# Completa los dos TODO (valores en texto plano - oc apply se encarga de
# codificarlos), y luego: oc apply -f manifests/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: podpet-credentials
  labels:
    app: podpet
    lab: lab03
type: Opaque
stringData:
  # TODO: define como "workshop"
  username: ""
  # TODO: define como "openshift123"
  password: ""
